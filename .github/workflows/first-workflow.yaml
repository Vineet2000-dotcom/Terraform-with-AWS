name: First workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome msg
        run: echo "My first job"

      - name: List files
        run: | 
             ls
             touch vineet.txt

